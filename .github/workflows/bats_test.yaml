name: Unit Test
on: 
  push:
    paths:
      - 'scripts/**'

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true no i

jobs:
  Bats-Test:
    runs-on: 'ubuntu-latest'
    
    permissions:
      id-token: write
      contents: read
      checks: write
[![Unit Test](https://github.com/bitovi/github-actions-gcp-bucket/actions/workflows/bats_test.yaml/badge.svg?event=check_run)](https://github.com/bitovi/github-actions-gcp-bucket/actions/workflows/bats_test.yaml)
    steps:
      - uses: actions/checkout@v3
        name: Checkout

      - id: 'auth'
        name: 'Authenticate to Google Cloud'
        uses: google-github-actions/auth@v1.1.0
        with:
          credentials_json: ${{ secrets.GOOGLE_CREDENTIALS }}

      - name: Setup BATS
        run: npm install -g bats

      - name: Run The Tests
        run: cd scripts/test && bats . --timing --report-formatter junit && cp report.xml ../../

      - name: Test Report
        uses: dorny/test-reporter@v1
        # run this step even if previous step failed
        if: success() || failure()
        with:
          name: JUNIT Tests     # Name of the check run which will be created
          path: report.xml      # Path to test results
          reporter: java-junit  # Format of test results
